package seeder

type MerkSeeder struct{}

// func (ms MerkSeeder) Run(db gorm.DB) {
// 	var (
// 		merkRepository repository.MerkRepository = repository.NewMerkRepository(db)
// 	)

// 	ASUS := repository.brandRepository.Find(model.Brand{brand: "ASUS"}).ID
// 	LENOVO := repository.brandRepository.Find(model.Brand{brand: "LENOVO"}).ID
// 	ACER := repository.brandRepository.Find(model.Brand{brand: "ACER"}).ID
// 	SAMSUNG := repository.brandRepository.Find(model.Brand{brand: "SAMSUNG"}).ID
// 	DELL := repository.brandRepository.Find(model.Brand{brand: "DELL"}).ID
// 	MAC := repository.brandRepository.Find(model.Brand{brand: "MAC"}).ID
// 	ZYREX := repository.brandRepository.Find(model.Brand{brand: "ZYREX"}).ID
// 	HP := repository.brandRepository.Find(model.Brand{brand: "HP"}).ID

// }
